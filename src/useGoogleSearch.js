import React from 'react';
import API_KEY from './keys';

const CONTEXT_KEY = "1e800dbd2644f9a80"

const UseGoogleSearch = (term) => {
    const [data, setData] = useState(null);

    useEffect(() => {
        

    }, [term]);
}

export default UseGoogleSearch;
